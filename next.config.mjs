/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['scontent.cdninstagram.com', 'instagram.com', 'scontent-lhr8-2.cdninstagram.com'],
  },
}

export default nextConfig
